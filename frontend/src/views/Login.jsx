import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import AuthShell from '../components/AuthShell.jsx';import PasswordField from '../components/PasswordField.jsx';import Captcha from '../components/Captcha.jsx';import api from '../utils/api.js';import {auth} from '../utils/auth.js';import './Login.css';
export default function Login({showMessage}){const nav=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[captcha,setCaptcha]=useState({});const submit=async e=>{e.preventDefault();try{const r=await api.post('/auth/login',{email,password,...captcha});auth.save(r.data.data);showMessage('success','Login successful');const role=r.data.data.user.role;nav(role==='admin'?'/admin/doctors/new':role==='patient'?'/patient/book':'/doctor/prescription')}catch(err){showMessage('error',err.response?.data?.message||'Login failed')}};return <AuthShell title="Welcome back" subtitle="Use one login. Your role is detected automatically."><form onSubmit={submit}><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><PasswordField label="Password" name="password" value={password} onChange={setPassword}/><Captcha onChange={setCaptcha}/><button className="btn">Login</button></form><div className="auth-links"><Link to="/register">Register patient</Link><Link to="/reset">Reset password</Link></div></AuthShell>}
