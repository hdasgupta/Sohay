import {useState} from 'react';import {PASSWORD_RULES} from '../utils/validation.js';import './PasswordField.css';
export default function PasswordField({value,onChange,name,label='Password',required=true}){const[show,setShow]=useState(false);return <div className="field"><label>{label}</label><div className="pass-wrap"><input required={required} name={name} type={show?'text':'password'} value={value} onChange={e=>onChange(e.target.value)} autoComplete="new-password"/><button type="button" onClick={()=>setShow(!show)}>{show?'🙈':'👁'}</button></div><div className="pass-rules">{PASSWORD_RULES.map(([text,ok])=><div key={text} className={ok(value)?'ok':'bad'}>{ok(value)?'✓':'✕'} {text}</div>)}</div></div>}
