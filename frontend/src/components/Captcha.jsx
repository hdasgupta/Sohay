import {useEffect,useState} from 'react';import api from '../utils/api.js';import './Captcha.css';
export default function Captcha({onChange}){const[data,setData]=useState(null);const[value,setValue]=useState('');const load=async()=>{try{const r=await api.get('/auth/captcha');setData(r.data.data);setValue('');onChange?.({id:r.data.data.id,answer:''})}catch(e){console.error('[captcha] load failed',e)}};useEffect(()=>{load()},[]);return <div className="captcha-box"><div className="captcha-image">{data&&<img src={data.image} alt="Visual CAPTCHA"/>}</div><button type="button" className="btn ghost captcha-refresh" onClick={load}>↻ Refresh</button><input aria-label="Captcha answer" placeholder="Enter captcha" value={value} onChange={e=>{setValue(e.target.value);onChange?.({id:data?.id,answer:e.target.value})}}/></div>}
